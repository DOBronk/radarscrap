The most easy way is to just run 'cmake . ' in the main directory, followed by make command if CMake was successful.

Requires:
- libcurl >1.12 
- OpenSSL >3.5 
- MySQL C++ Connector library (preferably 9.4.0)
- Zlib
